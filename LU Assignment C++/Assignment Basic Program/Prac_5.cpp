#include <bits/stdc++.h>
using namespace std;

int main()
{

    int side_a, side_b, side_c, formulaA, s;
    double perimeter;

    cout << "Enter the value of Side_a :";
    cin >> side_a;

    cout << "Enter the value of Side_b :";
    cin >> side_b;

    cout << "Enter the value of Side_c :";
    cin >> side_c;

    // perimeter = (side_a + side_b + side_c);
    // perimeter = perimeter * perimeter;
    s*s = side_a + side_b + side_c;
    // formulaA = sqrt(s(s-a)(s-b)(s-c));

    return 0;
}